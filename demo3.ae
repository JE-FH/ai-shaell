# Functions with closures
fn make_adder(n)
    fn adder(x)
        return x + n
    end
    return adder
end

let add5 = make_adder(5)
let add10 = make_adder(10)

"add5(100) = ${add5(100)}, add10(100) = ${add10(100)}"
